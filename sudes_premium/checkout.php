<?php include 'header.php';
$cart=$_SESSION['cart']??[]; if(!$cart){ header('Location: cart.php'); exit; }
$ids=array_keys($cart);$in=str_repeat('?,',count($ids)-1).'?';$st=$pdo->prepare("SELECT * FROM products WHERE id IN ($in)");$st->execute($ids);$items=$st->fetchAll();$total=0;foreach($items as $it){$total += product_price($it)*$cart[$it['id']];}
if($_SERVER['REQUEST_METHOD']==='POST'){$pdo->beginTransaction();$stmt=$pdo->prepare('INSERT INTO orders(customer_name,phone,email,address,note,total) VALUES(?,?,?,?,?,?)');$stmt->execute([$_POST['name'],$_POST['phone'],$_POST['email'],$_POST['address'],$_POST['note'],$total]);$oid=$pdo->lastInsertId();$oi=$pdo->prepare('INSERT INTO order_items(order_id,product_id,quantity,price) VALUES(?,?,?,?)');foreach($items as $it){$oi->execute([$oid,$it['id'],$cart[$it['id']],product_price($it)]);} $pdo->commit(); unset($_SESSION['cart']); header('Location: success.php?id='.$oid); exit;}
?>
<main class="container"><div class="section-title"><h2>Thanh toán</h2><p>Tổng đơn: <?=money($total)?></p></div><form class="form" method="post"><input name="name" required placeholder="Họ tên"><input name="phone" required placeholder="Số điện thoại"><input name="email" placeholder="Email"><textarea name="address" required placeholder="Địa chỉ giao hàng"></textarea><textarea name="note" placeholder="Ghi chú"></textarea><button class="btn">Đặt hàng</button></form></main><?php include 'footer.php'; ?>
